# Copy this file to credentials.yaml and fill in your values.
# credentials.yaml is gitignored — never commit real credentials.

copernicus:
  username: ""         # dataspace.copernicus.eu account
  password: ""         # or use env var COPERNICUS_PASSWORD

azure:
  subscription_id: ""
  resource_group: ""
  workspace_name: ""   # Azure ML workspace
  storage_account: ""
  storage_key: ""      # or use managed identity

azure_ad:
  tenant_id: ""
  client_id: ""
  client_secret: ""

mapbox:
  access_token: ""     # for the React dashboard
